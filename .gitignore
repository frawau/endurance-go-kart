*.pyc
*.DS_Store
*.egg*
/dist/
/.idea
/docs/_build/
/node_modules/
build/
env

#src
*.sqlite*

#venv
.beeware

# Environment variables with secrets
.env

# Database backups produced by `race-manager backup`
/backups/
gokart_backup_*.tar.gz

# Station runtime configs — derived from the *.toml.example templates by
# `race-manager configure-stations` and patched with .env values. The
# templates stay tracked; the runtime files are local-only so a git
# pull is never blocked by configuration changes.
/stations/timing/timing-station.toml
/stations/stopandgo/stopandgo-station.toml
